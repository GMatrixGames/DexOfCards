@page "/"
@using DexOfCards.Framework.Data

<main>
    <div class="container center">
        <div class="mt-3">
            <div>
                <div class="row gy-3">
                    @foreach (var card in DataStorage.GetCards(DataStorage.CardSets.First(a => a.SetId == "s8a-P_F")))
                    {
                        <div class="col-6 col-sm-4 col-md-3 col-lg-2 corner-radial text-center">
                            <div class="pokemon-card shadow-sm">
                                <div style="position: relative">
                                    <div class="menu-more">
                                        <img class="img-fluid card corner-radial" src="@card.CardImage" alt/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>
                @* <div class="row gy-3"> *@
                @*     @foreach (var card in DataStorage.CardSets) *@
                @*     { *@
                @*         <div class="col-6 col-sm-4 col-md-3 col-lg-2 corner-radial text-center"> *@
                @*             <div class="pokemon-card shadow-sm"> *@
                @*                 <div style="position: relative"> *@
                @*                     <div class="menu-more"> *@
                @*                         <img class="img-fluid card corner-radial" src="@card.SetImage" style="width: 30px;" alt/> *@
                @*                     </div> *@
                @*                 </div> *@
                @*             </div> *@
                @*         </div> *@
                @*     } *@
                @* </div> *@
            </div>
        </div>
    </div>
</main>

<!-- Width of Set images / Asia: 27px / US: 30 px -->